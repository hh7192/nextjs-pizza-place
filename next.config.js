/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  i18n: {
    locales: ["fa"],
    defaultLocale: "en-US",
  },
};

module.exports = nextConfig
